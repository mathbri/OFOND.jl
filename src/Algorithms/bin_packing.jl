# Bin packing functions

# All functions here should have a parameter to indicate whether the whole packing is 
# computed or if the curent state of the bins are to remain unchanged

function first_fit_decreasing()
    
end

function best_fit_decreasing()
    
end

function milp_packing()
    
end

# Local search where path are fixed and bin-packing are optimized again
function bin_packing_local_search()
    
end