# In all heuritisc you will code here, you will have :
# - an instance and some parameters as argument
# - a solution to store the current viable / feasible solution
# - objects, with some constituing the solution, on which you actually work 

# TODO : when starting to implement that, create another branch to deliver only the VO first than the V1 and so on 

# Maybe all all other objects that are useful to an AlgoUtils object ? 
# I think they will be the same for all heuristics

function lns_heuristic()
    # Combine the large perturbations and the small neighborhoods to design an efficient LNS
end